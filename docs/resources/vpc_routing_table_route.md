---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "stackit_vpc_routing_table_route Resource - stackit"
subcategory: ""
description: |-
  VPC routing table route resource schema.
---

# stackit_vpc_routing_table_route (Resource)

VPC routing table route resource schema.

## Example Usage

```terraform
resource "stackit_vpc_routing_table_route" "example" {
  project_id       = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
  vpc_id           = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
  region           = "eu01"
  routing_table_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

  destination = {
    type  = "cidrv4"
    value = "192.168.0.0/24"
  }
  next_hop = {
    type  = "ipv4"
    value = "192.168.0.10"
  }
  labels = {
    "key" = "value"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `destination` (Attributes) Destination of the regional routing table route. (see [below for nested schema](#nestedatt--destination))
- `next_hop` (Attributes) Next hop destination. (see [below for nested schema](#nestedatt--next_hop))
- `project_id` (String) STACKIT project ID to which the VPC is associated.
- `routing_table_id` (String) The routing table ID of the regional routing table route.
- `vpc_id` (String) The VPC ID to which the route is associated.

### Optional

- `labels` (Map of String) Labels are key-value string pairs, which can be attached to a resource container
- `region` (String) The resource region. If not defined, the provider region is used.

### Read-Only

- `created_at` (String) Date-time when the regional routing table route was created.
- `id` (String) Terraform's internal resource ID. It is structured as "`project_id`,`vpc_id`,`region`,`routing_table_id`,`route_id`".
- `route_id` (String) The ID of the regional routing table route.
- `updated_at` (String) Date-time when the regional routing table route was last updated.

<a id="nestedatt--destination"></a>
### Nested Schema for `destination`

Required:

- `type` (String) CIDRV type. Possible values are: `cidrv4`, `cidrv6`. Only `cidrv4` is supported currently.
- `value` (String) A CIDR string.


<a id="nestedatt--next_hop"></a>
### Nested Schema for `next_hop`

Required:

- `type` (String) Type of the next hop. Possible values are: `blackhole`, `internet`, `ipv4`, `ipv6`. Only `ipv4` supported currently.

Optional:

- `value` (String) Either IPv4 or IPv6 (not set for blackhole and internet). Only IPv4 supported currently.
